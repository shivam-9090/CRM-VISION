# ============================================================
# STAGING ENVIRONMENT CONFIGURATION
# ============================================================
# 
# ⚠️  FOR STAGING/TESTING ONLY - DO NOT USE IN PRODUCTION
# This file contains sample staging configuration.
# Copy to .env.staging and update with actual values.
#
# ============================================================

# Environment
NODE_ENV=staging

# Database Configuration (Staging Database)
POSTGRES_DB=crm_staging
POSTGRES_USER=crm_staging_user
POSTGRES_PASSWORD=GENERATE_STAGING_PASSWORD_$(openssl rand -base64 24)
DATABASE_URL=postgresql://crm_staging_user:CHANGE_THIS@staging-postgres:5432/crm_staging?schema=public&connection_limit=5&pool_timeout=10

# Redis Configuration (Staging Redis)
REDIS_HOST=staging-redis
REDIS_PORT=6379
REDIS_URL=redis://staging-redis:6379

# JWT Configuration (Use different secret than production)
JWT_SECRET=STAGING_JWT_SECRET_$(openssl rand -base64 48)
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
PORT=3001
FRONTEND_URL=https://staging.your-domain.com

# Frontend Configuration
NEXT_PUBLIC_API_URL=https://staging-api.your-domain.com/api
NEXT_PUBLIC_APP_NAME="CRM Vision (Staging)"
NEXT_PUBLIC_APP_VERSION="1.0.0-staging"

# Sentry Error Monitoring (Optional for staging)
# Use separate Sentry project for staging
SENTRY_DSN=https://staging-sentry-dsn@sentry.io/staging-project-id
SENTRY_RELEASE=crm-backend@staging
SENTRY_ENVIRONMENT=staging
SENTRY_ENABLE_DEV=true

# Email Configuration (Use test email service)
# Mailtrap, Mailhog, or separate staging SMTP
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-mailtrap-username
SMTP_PASS=your-mailtrap-password
EMAIL_FROM=noreply-staging@your-domain.com

# Logging
LOG_LEVEL=debug

# ============================================================
# STAGING-SPECIFIC NOTES:
# 
# 1. Use separate database from production
# 2. Use test email service (Mailtrap/Mailhog)
# 3. Enable debug logging
# 4. Use different JWT secret
# 5. Configure separate Sentry project
# 6. Use staging subdomain (staging.your-domain.com)
# 
# ============================================================

# ============================================================
# RENDER STAGING SERVICES CONFIGURATION:
# 
# After creating staging services on Render, add their IDs
# to GitHub Secrets:
# 
# - RENDER_SERVICE_ID_BACKEND_STAGING
# - RENDER_SERVICE_ID_FRONTEND_STAGING
# - DATABASE_URL_STAGING
# 
# ============================================================
