# ============================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================
# 
# ⚠️  SECURITY WARNING: 
# DO NOT COMMIT THIS FILE TO GIT!
# Add to .gitignore immediately
#
# Generate strong secrets using:
# openssl rand -base64 64
# ============================================================

# Database Configuration
POSTGRES_DB=crm_production
POSTGRES_USER=crm_admin
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD_$(openssl rand -base64 32)

# Backend Configuration
DATABASE_URL=postgresql://crm_admin:CHANGE_THIS@postgres:5432/crm_production?schema=public&connection_limit=10&pool_timeout=20
REDIS_URL=redis://redis:6379
JWT_SECRET=CHANGE_THIS_TO_STRONG_JWT_SECRET_$(openssl rand -base64 64)
JWT_EXPIRES_IN=1d
NODE_ENV=production
PORT=3001

# Sentry Error Monitoring (REQUIRED for production)
# Sign up at https://sentry.io (free tier available)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Frontend URL (update with your actual domain)
FRONTEND_URL=https://your-domain.com

# Frontend Configuration (update with your actual API URL)
NEXT_PUBLIC_API_URL=https://api.your-domain.com/api
NEXT_PUBLIC_APP_NAME="CRM Vision"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# ============================================================
# SETUP INSTRUCTIONS:
# 
# 1. Copy this file:
#    cp .env.production.example .env.production
#
# 2. Generate strong passwords:
#    POSTGRES_PASSWORD: openssl rand -base64 32
#    JWT_SECRET: openssl rand -base64 64
#
# 3. Update DATABASE_URL with the same password
#
# 4. Update FRONTEND_URL and NEXT_PUBLIC_API_URL
#    with your actual domain names
#
# 5. Ensure .env.production is in .gitignore
#
# 6. Deploy with:
#    docker-compose -f docker-compose.prod.yml up -d
# ============================================================
