# Railway.app Configuration for CRM Backend
# Note: In Railway dashboard, set Root Directory to "backend"

[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
